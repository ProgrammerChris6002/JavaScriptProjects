				Tests

1. If (.firstname-input contains anything other than [a-zA-Z0-9] or 
   length is < 3 or length is > 16) then:
	.firstname-p content = First name must be alphanumeric and 
	contain 3 - 16 characters
	style input - outline & border = red
2. If (.lastname-input contains anything other than [a-zA-Z0-9] or 
   length is < 3 or length is > 16) then:
	.lastname-p content = Last name must be alphanumeric and 
	contain 3 - 16 characters
	style input - outline & border = red
3. If (.email-input contains anything other than [a-zA-Z0-9@.]) then:
	.email-p content = Email must a valid address, e.g.
	example@example.com
	style input - outline & border = red
4. (If .password-input contains anything other than [a-zA-Z0-9@_-] or length is < 6 or length is > 20)then:
	.password-p content = Password must be alphanumeric and 
	contain 6 - 20 characters
	style input - outline & border = red
5. (If .telephone-input length != 11) or is not numeric then:
	.telephone-p content = A valid Telephone number 
	(11 digits and 333-333-3334)
6. (If .your-bio-input contains anything other than [a-zA-Z0-9@_-] or length is < 8 or length is > 50)then:
	.your-bio-p content = Bio must contain only lowercase letters, 
	underscores, hyphens and be 8 - 50 characters
	style input - outline & border = red
7. else 
	style input - outline & border = green

addevent: if all tests passed then:
	style button: bacCol, opacity, enable



		Functions

1. validateInputLength(element, min, max) - Returns T or F.
2. validateInputContent(element, check, alphanumeric, extraChars='') - Returns T or F.
3. validateInput(element, check, errorMsg)
3. validateNameInput() - 1 & 2
4. validateEmailInput() - 3
5. validatePasswordInput() - 4
6. validateTelephoneInput() - 5
7. validateBioInput() - 6






Devtobs.scholarship@gmail.com